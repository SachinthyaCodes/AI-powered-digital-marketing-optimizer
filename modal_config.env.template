# Modal Configuration for RAG System
# Add these to your .env file

# Modal Endpoints (get these from your Modal deployment)
# MODAL_EMBEDDING_URL=https://your-modal-app.modal.run/embed
# MODAL_GENERATE_URL=https://your-modal-app.modal.run/generate
# MODAL_CHAT_URL=https://your-modal-app.modal.run/chat

# For now (testing without Modal):
# Leave blank to use fallback mode
MODAL_EMBEDDING_URL=
MODAL_GENERATE_URL=
MODAL_CHAT_URL=

# HuggingFace Token (for SinLlama tokenizer - already configured in code)
# HF_TOKEN=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

# Note: System will work without Modal (uses fallback for generation)
# FAQs will still be extracted and documents will be indexed properly
